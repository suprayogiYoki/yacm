# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#
# pre-push:
#   jobs:
#     - name: packages audit
#       tags:
#         - frontend
#         - security
#       run: yarn audit
#
#     - name: gems audit
#       tags:
#         - backend
#         - security
#       run: bundle audit
#
# pre-commit:
#   parallel: false
#   commands:
#     1. make prisma scheme:
#       run: npx tsx src/backend/lib/run_merge_prisma.ts
#     2. format prisma fix relation:
#       run: npx prisma format --schema=src/backend/prisma/schema.prisma
#     3. generate prisma code:
#       run: npx prisma generate --schema=src/backend/prisma/schema.prisma
    # 4. migrate prisma db:
    #   run: echo 'auto' | npx prisma migrate dev --schema=src/backend/prisma/schema.prisma

    # - run: yarn eslint {staged_files}
    #   glob: "*.{js,ts,jsx,tsx}"

    # - name: rubocop
    #   glob: "*.rb"
    #   exclude:
    #     - config/application.rb
    #     - config/routes.rb
    #   run: bundle exec rubocop --force-exclusion {all_files}

    # - name: govet
    #   files: git ls-files -m
    #   glob: "*.go"
    #   run: go vet {files}

    # - script: "hello.js"
    #   runner: node

    # - script: "hello.go"
    #   runner: go run
